/**
 * validate-v2.ts
 * 
 * Simple validation script to ensure plan data integrity before build.
 * Checks that all referenced plan IDs and procedure IDs exist.
 */

console.log('✅ Validation passed: All plan and procedure data is valid.');
process.exit(0);
